---
import Container from '@/components/Container.astro';
import { NAV_LINKS, SITE } from '@/consts';
---

<header class="sticky top-0 z-50 border-b border-surface-muted bg-surface/85 backdrop-blur-md">
  <Container class="flex items-center justify-between gap-6 py-4">
    <a
      href="/"
      class="font-display text-lg font-bold tracking-tight text-ink transition-colors hover:text-signal-text"
    >
      {SITE.name}<span class="text-signal">.</span>
    </a>

    <nav aria-label="Primary">
      <ul class="flex items-center gap-4 sm:gap-8">
        {
          NAV_LINKS.map(({ href, label }) => (
            <li>
              <a
                href={href}
                class="font-mono text-xs uppercase tracking-label text-ink-muted transition-colors hover:text-signal-text"
              >
                {label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </Container>
</header>
